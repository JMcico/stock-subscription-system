# Copy to .env and adjust. When POSTGRES_DB is set, Django uses PostgreSQL; otherwise SQLite.
# POSTGRES_DB=stock_subscription
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
